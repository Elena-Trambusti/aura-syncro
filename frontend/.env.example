# Sviluppo locale — opzionale: il frontend in DEV usa il proxy Vite (/api → :3001) per evitare CORS.
# Usa VITE_API_URL solo se il proxy non è disponibile (es. build preview senza proxy).
# VITE_API_URL="http://localhost:3001"

# Produzione Vercel (https://aurasyncro.com) — imposta in Vercel → Settings → Environment Variables:
# BACKEND_URL="https://aura-syncro-s98ae.ondigitalocean.app"
# (opzionale, per WebSocket diretto) VITE_API_URL="https://aura-syncro-s98ae.ondigitalocean.app"

VITE_STRIPE_PUBLISHABLE_KEY="pk_test_..."

# Dati legali titolare (opzionale — informative privacy/termini)
# VITE_LEGAL_STREET_ADDRESS="Via Esempio 1"
# VITE_LEGAL_ZIP_CODE="57100"
# VITE_LEGAL_PEC="titolare@pec.it"

# Sentry (error tracking frontend) — da Sentry → Settings → Client Keys (DSN)
# ATTENZIONE: copia il DSN esatto dal wizard Sentry (step 2), carattere per carattere
VITE_SENTRY_DSN="https://f0fb781948d080042328e24e820df515@o4511618999451648.ingest.de.sentry.io/4511619023175760"

