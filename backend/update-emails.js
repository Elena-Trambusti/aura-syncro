const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const users = await prisma.user.findMany({ where: { email: { contains: 'admin@demo.' } } }); for (const user of users) { if (user.email.endsWith('.es-cn')) { await prisma.user.update({ where: { id: user.id }, data: { email: 'admin@demo-es-cn.com' } }); } else if (user.email.endsWith('.es')) { await prisma.user.update({ where: { id: user.id }, data: { email: 'admin@demo-es.com' } }); } else if (user.email.endsWith('.it')) { await prisma.user.update({ where: { id: user.id }, data: { email: 'admin@demo-it.com' } }); } } console.log('Emails updated successfully.'); } main().catch(console.error).finally(() => prisma.$disconnect());
